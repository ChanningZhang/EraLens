services:
  postgres:
    image: postgres:16-alpine
    container_name: eralens-postgres
    environment:
      POSTGRES_USER: eralens
      POSTGRES_PASSWORD: eralens
      POSTGRES_DB: eralens
    ports:
      - "5432:5432"
    volumes:
      - eralens_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U eralens -d eralens"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  eralens_pg_data:
