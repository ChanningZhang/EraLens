# 技术规范
1. 绝大部分逻辑都必须基于抽象规则，而非特殊处理。
2. 通过抽象规则+数据上的标记来处理大多数例外。
3. 代码层面所有的特殊处理逻辑都必须我同意。
4. 所有的王朝名、泳道、帝王卡片、tooltip、字体大小、谥号庙号年号等展示方式，都需以组件的方式共用，不允许对某个对象特殊处理。

# 业务规范
1. 同一王朝的帝王，结束和开始时间相同的，默认按时间先后串行排列，除非有特殊标记（一朝多帝）。
2. 对于上古帝王仅只排列顺序，没有明确时间的，存入数据库时按帝王数量平分相应的时间段。
   
   